#include "main.h"

/**
 *The _islower - checks the code for lowercase character
 * c: For the character to check
 * Return: 1 if c is lowercase, 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
